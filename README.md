# SMD Model Calibration Repository

This repository contains input, output, and observation files for a distributed hydrological model
that simulates **Soil Moisture Deficit (SMD)** across five reaches of the **Bosque** catchment.
Accompanying R scripts provide a complete pipeline for adding timestamps to raw model output,
matching it against field observations, and computing goodness-of-fit statistics.

> **Repository:** https://github.com/martynfutter/SMD_Calibration

---

## Repository Contents

```
Data files
├── R1_01.par  R2_01.par  R3_01.par  R4_01.par  R5_01.par   Model parameter files (one per reach)
├── R1_30.obs  R2_30.obs  R3_30.obs  R4_30.obs  R5_30.obs   SMD observation files (30-min, one per reach)
└── r1.dat                                                    Raw model result file (Reach 1, Run 01)

Analysis scripts
├── smd_pipeline.R              End-to-end pipeline (recommended entry point)
├── add_datetime_to_results.R   Step 1 – prepend timestamps to raw .dat output
├── match_smd.R                 Step 2 – match modelled SMD against observations
├── smd_statistics.R            Step 3 – compute goodness-of-fit statistics
├── smd_analysis.R              Multi-reach analysis across all reaches
└── run_persist.R               Persistent run helper

Configuration
└── smd_config.json             Run configuration (file paths, column indices)

Intermediate outputs (generated by scripts – not tracked in git)
├── r1_dates.dat                Timestamped model output from Step 1
├── r1_30_match.dat             Matched obs/modelled pairs from Step 2
└── R1_30_match_statistics.txt  Goodness-of-fit statistics from Step 3

Project files
├── SMD.Rproj   R project file
├── README.md   This file
└── .gitignore
```

---

## File Format Reference

### Parameter files (`.par`)

Each `.par` file configures a single model run.
The naming convention is `R<ReachID>_<RunID>.par` (e.g. `R1_01.par` = Reach 1, Run 01).

| Line | Content                                     | Example               |
|------|---------------------------------------------|-----------------------|
| 1    | Time step in seconds                        | `1800`                |
| 2    | Number of time steps                        | `72838`               |
| 3    | Run start date/time (`DD/MM/YYYY HH:MM:SS`) | `01/10/2021 00:00:00` |
| 4+   | Model parameters by component               | —                     |

The parameter blocks, in order, are:

- **Quick** – fast (surface/near-surface) runoff store
- **QuickRip** – fast runoff store for the riparian zone
- **Soil** – soil water store
- **SoilRip** – soil water store for the riparian zone
- **Groundwater** – groundwater/baseflow store
- **RAP** – reach routing and vegetation parameters

Each store typically contains paired values representing two sub-reaches or land-use zones.
The final lines of the `RAP` block define the routing matrix between sub-reaches.

### Observation files (`.obs`)

Each `.obs` file contains field-measured SMD values for a single reach.
The naming convention is `R<ReachID>_30.obs` (e.g. `R1_30.obs` = Reach 1, 30-minute interval).

**Structure:**

```
************** Reach <name> ***********
-------------- <parameter> -----------
DD/MM/YYYY HH:MM:SS <value>
DD/MM/YYYY HH:MM:SS <value>
...
```

- Line 1: reach identifier (e.g. `Reach RAP`)
- Line 2: parameter name (all files in this repository use `SMD`)
- Lines 3+: space-delimited observations — date, time, numeric value

Only files where line 2 contains `SMD` are processed by the analysis scripts.

**Example (from `R1_30.obs`):**

```
************** Reach RAP ***********
-------------- SMD -----------
18/12/2024 12:00:00 6.15
18/12/2024 12:30:00 6.15
...
```

Observations span from **18 December 2024** onwards at **30-minute** intervals,
giving approximately 17 600 records per reach.

### Model result files (`r*.dat`)

Raw model output files are named `r<RunID>.dat` (e.g. `r1.dat`).
They are space-delimited with **no header** and **no timestamps**.
Each row corresponds to one time step; the first row corresponds to
`start_time + 1 × time_step`.

The file contains four columns:

| Column | Content                 |
|--------|-------------------------|
| 1      | Simulated flow (cumecs) |
| 2      | Reserved (always 0)     |
| 3      | **Simulated SMD (mm)**  |
| 4      | Reserved (always 0)     |

Timestamps must be reconstructed from the corresponding `.par` file
before the results can be compared with observations (see pipeline below).

---

## Reaches

The repository currently contains data for **five reaches** within the Bosque catchment:

| Reach ID | Parameter file | Observation file | Model result |
|----------|---------------|-----------------|--------------|
| R1       | R1_01.par     | R1_30.obs       | r1.dat       |
| R2       | R2_01.par     | R2_30.obs       | —            |
| R3       | R3_01.par     | R3_30.obs       | —            |
| R4       | R4_01.par     | R4_30.obs       | —            |
| R5       | R5_01.par     | R5_30.obs       | —            |

All runs share the same time step (1800 s) and start date (01 October 2021).

---

## Analysis Pipeline

The recommended workflow is the single consolidated script `smd_pipeline.R`.
Alternatively, the three standalone scripts can be run in sequence.

### Quick start

1. Clone or download the repository and open `SMD.Rproj` in RStudio,
   or set the working directory to the repository root.
2. Edit the **USER CONFIGURATION** block at the top of `smd_pipeline.R`:

```r
par_file       <- "R1_01.par"   # .par file for this run
result_file    <- "r1.dat"      # raw model result file
obs_file       <- "R1_30.obs"   # observation file (SMD)
smd_col_in_dat <- 3             # column number of SMD in the .dat file
```

3. Run the script:

```r
source("smd_pipeline.R")
# or from a terminal:
# Rscript smd_pipeline.R
```

No additional R packages are required (base R only).

### Pipeline steps

| Step | Script                      | Description                                                                                              | Output                      |
|------|-----------------------------|----------------------------------------------------------------------------------------------------------|-----------------------------|
| 1    | `add_datetime_to_results.R` | Reads time step and start date from `.par`; prepends `Date` and `Time` columns to the `.dat` file       | `r1_dates.dat`              |
| 2    | `match_smd.R`               | Extracts SMD from the dated model file; inner join with `.obs` on `(Date, Time)`                         | `r1_30_match.dat`           |
| 3    | `smd_statistics.R`          | Computes R², Nash–Sutcliffe Efficiency, and Kling–Gupta Efficiency                                       | `R1_30_match_statistics.txt`|
| 4    | *(pipeline only)*           | Produces a time-series comparison plot                                                                   | `R1_30_plot.png`            |

### Multi-reach analysis

`smd_analysis.R` runs the full pipeline across all available reach/result combinations
and assembles a summary statistics table.  Edit the reach list at the top of the script
to control which reaches are processed.

### Goodness-of-fit statistics

| Statistic | Perfect score | Notes                                                                                                            |
|-----------|---------------|------------------------------------------------------------------------------------------------------------------|
| R²        | 1.0           | Coefficient of determination                                                                                     |
| NS        | 1.0           | Nash–Sutcliffe Efficiency; 0 = no better than the observed mean; negative = poor                                 |
| KGE       | 1.0           | Kling–Gupta Efficiency (Gupta et al., 2009); values > −0.41 outperform the mean benchmark (Knoben et al., 2019) |

---

## Model Overview

The model simulates the hydrological behaviour of the **Bosque** catchment using a
distributed structure with five reaches (R1–R5).  Each reach represents two zones
(main channel and riparian) with separate fast-flow, soil-water, and groundwater stores
that drain through a routing matrix into the river reach.

Key run parameters for the files in this repository:

| Parameter            | Value                   |
|----------------------|-------------------------|
| Time step            | 1800 s (30 min)         |
| Start date           | 01 October 2021 00:00   |
| Number of steps      | 72 838                  |
| Approximate end date | ~December 2025          |
| Observation period   | 18 December 2024 –      |

---

## References

Gupta, H. V., Kling, H., Yilmaz, K. K., & Martinez, G. F. (2009).
Decomposition of the mean squared error and NSE performance criteria:
Implications for improving hydrological modelling.
*Journal of Hydrology*, 377(1–2), 80–91.

Knoben, W. J. M., Freer, J. E., & Woods, R. A. (2019).
Technical note: Inherent benchmark or not? Comparing Nash–Sutcliffe and
Kling–Gupta efficiency scores.
*Hydrology and Earth System Sciences*, 23(10), 4323–4331.
